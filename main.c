#include <gb/gb.h> // If "gb/gb.h" file is not found, make sure the path to the gbdk include folder is set properly in ".vscode\c_cpp_properties.json"
#include <stdio.h>

// Press F5 to compile and run the compiled game.gb in the Emulicous Emulator/Debugger
// Note: if it fails, make sure the Emulicous Emulator and the Emulicous VS Code extension is installed (See: README.md for more infos)
void main(void)
{
    printf("Hello World");
}